<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.seciii.oasis.data.keyword.KeywordDetailMapper">

    <select id="searchRelatedKeyword" resultMap="Keyword">
        select * from keyword
        where kid in
              (select kid from keywordpaper,
                  (select pid from keywordpaper where kid = #{kid}) p
              where p.pid = keywordpaper.pid and kid!=#{kid})
    </select>

    <select id="searchCollaboration" resultMap="KeywordCol">
        select kid, count(*) as collaborCount from keywordpaper
        where pid in
        (select pid from keywordpaper where kid = #{kid})
        and keywordpaper.kid != #{kid} group by kid;
    </select>

    <select id="selectAuthorByKid" resultType="int">
        select distinct aid from authorpaper
        where pid in
              (select pid from keywordpaper where kid = #{kid})
    </select>

    <select id="selectInstitutionByKid" resultType="int">
        select distinct iid from institutionpaper
        where pid in
              (select pid from keywordpaper where kid = #{kid})
    </select>

    <select id="selectPaperByKid" resultType="int">
        select pid from keywordpaper where kid = #{kid}
    </select>

    <resultMap id="Keyword" type="com.seciii.oasis.po.Keyword">
        <id column="kid" property="kid"></id>
        <result column="kname" property="kname"></result>
        <result column="activity" property="activity"></result>
        <result column="paperamount" property="paperamount"></result>
    </resultMap>

    <resultMap id="KeywordCol" type="com.seciii.oasis.po.KeywordCol">
        <id column="kid" property="kid"></id>
        <result column="collaborCount" property="collaborCount"></result>
    </resultMap>

</mapper>